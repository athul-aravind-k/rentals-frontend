export interface SideNavToggle {
  screenWidth: number;
  collapsed: boolean;
}

export interface MenuSelectorModel {
  label: string;
  iconClass: string;
  path: string;
}

export interface SideNavToggle {
  screenWidth: number;
  collapsed: boolean;
}
